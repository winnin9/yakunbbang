# 야근빵 — 런칭 To Do

> 완료된 항목은 `- [x]`로 체크. 작업 시작 전 브랜치 따서 PR로 머지 권장.

---

## 🔴 런칭 필수

- [x] **앱인토스 SDK 연동** — `src/hooks/useStorage.ts`의 localStorage를 Toss Storage API로 교체
- [x] **앱인토스 공유 API 연동** — `src/pages/ReportPage.tsx`의 이미지 다운로드 방식을 Toss 공유 API로 교체
- [x] **앱인토스 개발 환경 세팅** — 미니앱 SDK 설치, WebView 환경 로컬 테스트 (granite.config.ts 생성 필요)

---

## 🟡 오픈 전 권장

- [x] **공유 카드 디자인 시안** — 식빵이 반투명 이미지 + 그라디언트 배경 + 워터마크 적용 완료
- [x] **제빵사 등급 카피라이팅 확정** — 5단계 문구 확정 (전설의 제빵사 / 믿음직한 제빵사 / 노력파 제빵사 / 까맣게 타는 중 / 야근 마왕)
- [ ] **html2canvas 이모지 테스트** — iOS/Android 실기기에서 👨‍🍳 등 복합 이모지 공유 카드 렌더링 확인
- [ ] **모바일 실기기 최종 확인** — 아래 `.ait` 빌드 & 업로드 절차로 토스앱에서 직접 테스트
  ```
  # 1. 빌드
  npm run build   → 루트에 yakunbbang.ait 생성

  # 2. 콘솔 업로드 (수동)
  콘솔 → 워크스페이스 → 앱 선택 → 앱 출시 → .ait 업로드 → QR 스캔

  # 2. 또는 CLI 배포 (자동)
  npx ait token add
  npx ait deploy

  # 3. 토스앱에서 테스트
  스킴: intoss-private://appsintoss?_deploymentId={발급ID}
  조건: 토스앱 로그인 + 워크스페이스 멤버 + 만 19세 이상 필수
  주의: 광고 테스트는 콘솔 QR 코드로만 가능 (샌드박스 미지원)
  ```

---

## 🟢 오픈 후 (MVP 후속)

- [x] **빵 스킨 선택 기능** — 홈에서 스킨 선택 UI 추가. `src/types/index.ts`의 `BREAD_SKIN_IMAGE` 맵에 스킨 추가하면 됨
- [ ] **세션 삭제 기능** — 잘못 기록한 세션 삭제 (오븐 리스트에서 스와이프 삭제 등)
- [x] **리워드 광고 — 1단계 (세션당 잠금)** — 식빵이 외 스킨 선택 시 광고 시청 후 해당 세션 1회 사용
  ```
  광고 타입: 보상형(Rewarded) — 테스트 ID: ait-ad-test-rewarded-id
  SDK: @apps-in-toss/web-bridge 이미 설치됨
  흐름: 스킨 선택 → 광고 로드 → 시청 완료 콜백 → 해당 세션 스킨 적용
  주의: 서비스 진입 직후 광고 노출 금지 / 결제·인증 흐름 중 삽입 금지
  테스트: 콘솔 QR 코드로만 가능 (샌드박스 환경에서 광고 미지원)
  ```
- [ ] **리워드 광고 — 2단계 (영구 해금)** — 1단계 반응 확인 후 도입. 광고 1회 시청으로 해당 스킨 영구 해금, 해금 상태 Toss Storage에 저장
- [ ] **친구 비교 기능** — 공유 반응 확인 후 도입 검토
- [ ] **앱 내 푸시 알림** — 목표 시간 도달 시 알림

---

## ✅ 완료

- [x] 세션 유실 방지 (새로고침/앱 재시작 시 active 세션 복원)
- [x] 자정 넘기는 야근 자동 보정
- [x] 음수 초과율 긍정 피드백 ("일찍 완료")
- [x] localStorage 저장 실패 알림
- [x] 중복 세션 방지
- [x] OvenList 타임존 날짜 파싱 버그 수정
- [x] ReportPage 자정 이후 월 기준 고정 버그 수정
- [x] 아이콘 inline SVG로 통일 (뒤로가기, 월 이동, 오븐)
- [x] 오븐 리스트 썸네일 → 빵 이미지 + 상태 이모지 뱃지
- [x] 분 단위 10분 → 5분 세분화
